apiVersion: apps/v1
kind: Deployment
metadata:
  name: badminton-rotator
  labels:
    app: badminton-rotator
spec:
  replicas: 6
  selector:
    matchLabels:
      app: badminton-rotator
  template:
    metadata:
      labels:
        app: badminton-rotator
    spec:
      containers:
      - name: web
        image: localhost/badminton-rotator-app:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: badminton-rotator-service
spec:
  type: NodePort
  selector:
    app: badminton-rotator
  ports:
  - port: 80
    targetPort: 3000
    nodePort: 30080
